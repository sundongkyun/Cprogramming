/*
#include <stdio.h>

int main(void) {
	int inputSecond, hour, min, second;

	printf("ÃÊ(second)ÀÔ·Â: ");
	scanf("%d", &inputSecond);

	hour = inputSecond / 3600;
	min = (inputSecond % 3600) / 60;
	second = (inputSecond % 3600) % 60;

	printf("[h: %d, m: %d, s: %d]",hour, min, second);

	return 0;
}
*/