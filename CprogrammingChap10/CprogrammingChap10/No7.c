/*
#include <stdio.h>
#include <math.h>

int main(void) {
	int n;
	int k = 1;
	
	printf("상수 n 입력: ");
	scanf("%d", &n);
	printf("\n");

	while (pow(2,k) <= n) {
		k++;
	}

	printf("공식을 만족하는 k의 최댓값은 %d\n", k-1);


	return 0;
}
*/
/*
#include <stdio.h>

int main(void) {
	int n;
	int k = 1;
	int multi = 1;

	printf("상수 n 입력: ");
	scanf("%d", &n);
	printf("\n");

	while ( 2*multi <= n) {
		multi = multi * 2;
		k++;
	}

	printf("공식을 만족하는 k의 최댓값은 %d\n", k - 1);


	return 0;
}
*/