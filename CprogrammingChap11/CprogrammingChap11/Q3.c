/*
#include <stdio.h>

int main(void) {

	char arr[100];
	int length = 0;

	printf("한개의 영단어를 입력하시오.");
	scanf("%s", arr);

	for (int i = 0; i < 100; i++) {
		if (arr[i] == '\0') {
			break;
		}
		else {
			length++;
		}
	}

	printf("영단어 길이는: %d\n",length);

	return 0;
}
*/